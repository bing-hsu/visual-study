export interface Margin {
  top: number;
  bottom: number;
  left: number;
  right: number;
}

// SVG dimension
export interface Dimension {
  x: number;
  y: number;
  width: number;
  height: number;
}
